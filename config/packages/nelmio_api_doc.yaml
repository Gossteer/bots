nelmio_api_doc:
    documentation:
        info:
            title: '%app_name%'
            description: '%app_name%'
            version: 1.0.0
    areas: # to filter documented areas
        default:
            path_patterns:
                - ^/api # Accepts routes under /api except /api/doc
                - ^/healthCheck
